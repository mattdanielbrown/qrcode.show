name = "qrcode"
main = "build/index.js"


[build]
command = "cargo install worker-build@^0.8 && worker-build --release" # required

# read more about configuring your Worker via wrangler.toml at:
# https://developers.cloudflare.com/workers/cli-wrangler/configuration
