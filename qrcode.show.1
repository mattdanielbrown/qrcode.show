.\" Automatically generated by Pandoc 3.7.0.2
.\"
.TH "QRcode.show" "1" "" ""
.SS NAME
\f[B]QRcode.show \- Generate QR code easily for free \- QR Code
Generation as a Service\f[R]
.SS USAGE
.IP
.EX
curl qrcode.show/INPUT
.EE
.IP
.EX
curl qrcode.show \-d INPUT
.EE
.IP
.EX
curl qrcode.show \-d \(at/PATH/TO/INPUT
.EE
.IP
.EX
echo INPUT \f[B]|\f[R] curl qrcode.show \-d \(at\-
.EE
.SS USAGE EXAMPLES
.IP
.EX
curl qrcode.show/https://example.com
.EE
.IP
.EX
curl qrcode.show \-d https://example.com
.EE
.IP
.EX
curl qrcode.show \-d \(at/path/to/input.txt
.EE
.IP
.EX
echo https://example.com \f[B]|\f[R] curl qrcode.show \-d \(at\-
.EE
.SS PARAMETERS
\f[B]Accept\f[R]
.PP
Specify the output type.
Options: \f[CR]application/octet\-stream\f[R] (default) /
\f[CR]text/plain\f[R] / \f[CR]text/html\f[R] / \f[CR]image/svg+xml\f[R]
/ \f[CR]image/png\f[R] / \f[CR]image/jpeg\f[R]
.PP
\f[B]X\-QR\-Width\f[R]
.PP
Specify the default width.
.PP
\f[B]X\-QR\-Height\f[R]
.PP
Specify the default height.
.PP
\f[B]X\-QR\-Min\-Width\f[R]
.PP
Specify the minimum width.
.PP
\f[B]X\-QR\-Min\-Height\f[R]
.PP
Specify the minimum height.
.PP
\f[B]X\-QR\-Max\-Width\f[R]
.PP
Specify the maximum width.
.PP
\f[B]X\-QR\-Max\-Height\f[R]
.PP
Specify the maximum height.
.PP
\f[B]X\-QR\-Dark\-Color\f[R]
.PP
Specify the dark color (hex).
Format: rrggbb
.PP
\f[B]X\-QR\-Light\-Color\f[R]
.PP
Specify the light color (hex).
Format: rrggbb
.PP
\f[B]X\-QR\-Version\-Type\f[R]
.PP
Specify the QR version type.
Options: normal / micro
.PP
\f[B]X\-QR\-Version\-Number\f[R]
.PP
Specify the QR version number.
Options: \f[CR]1\f[R] \-> \f[CR]40\f[R] for normal, \f[CR]1\f[R] \->
\f[CR]4\f[R] for micro.
.PP
\f[B]X\-QR\-EC\-Level\f[R]
.PP
Specify the error checking level.
Options: \f[CR]L\f[R] (default) / \f[CR]M\f[R] / \f[CR]Q\f[R] /
\f[CR]H\f[R]
.PP
\f[B]X\-QR\-Quiet\-Zone\f[R]
.PP
Specify whether the quiet zone is added.
Options: \f[CR]true\f[R] (default) / \f[CR]false\f[R]
.SS PARAMETER EXAMPLES
.IP
.EX
curl qrcode.show/INPUT \-H \(dqAccept: image/svg+xml\(dq
.EE
.SS SHELL FUNCTIONS
Shell functions that can be added to \f[CR].bashrc\f[R] or
\f[CR].bash_profle\f[R] for quickly generating QR codes from the command
line.
The command takes the argument as input or reads from stdin if none was
supplied and outputs the QR code to stdout.
e.g.\ \f[CR]qrcode INPUT\f[R] or \f[CR]echo INPUT | qrcode\f[R]
.IP
.EX
qrcode () \f[B]{\f[R]
    local input=\(dq$*\(dq
    [ \-z \(dq$input\(dq ] \f[B]&&\f[R] local input=\(dq\(at/dev/stdin\(dq
    curl \-d \(dq$input\(dq https://qrcode.show
\f[B]}\f[R]
qrsvg () \f[B]{\f[R]
    local input=\(dq$*\(dq
    [ \-z \(dq$input\(dq ] \f[B]&&\f[R] local input=\(dq\(at/dev/stdin\(dq
    curl \-d \(dq${input}\(dq https://qrcode.show \-H \(dqAccept: image/svg+xml\(dq
\f[B]}\f[R]
qrserve () \f[B]{\f[R]
    local port=${1:\-8080}
    local dir=${2:\-.}
    local ip=\(dq$(ifconfig \f[B]|\f[R] grep \-Eo \(aq[0\-9]{1,3}\(rs.[0\-9]{1,3}\(rs.[0\-9]{1,3}\(rs.[0\-9]{1,3}\(aq \f[B]|\f[R] fzf \-\-prompt IP:)\(dq \(rs
    \f[B]&&\f[R] echo http://$ip:$port \f[B]|\f[R] qrcode \(rs
    \f[B]&&\f[R] python \-m http.server $port \-b $ip \-d $dir
\f[B]}\f[R]
.EE
.SS FEATURES
.IP \(bu 2
No data collection or retention.
.IP \(bu 2
Fast and simple API that works on both web and terminal.
.IP \(bu 2
Supports GET and POST requests.
.IP \(bu 2
Supports \f[CR]Accept\f[R] header to control the output format.
.SS CREDITS
.IP \(bu 2
\f[B]Main Library\f[R]: \c
.UR https://github.com/kennytm/qrcode-rust
.UE \c
.IP \(bu 2
\f[B]Cloudflare Worker\f[R] : \c
.UR https://github.com/cloudflare/workers-rs
.UE \c
.IP \(bu 2
\f[B]Alternate Web Server\f[R]: \c
.UR https://github.com/tokio-rs/axum
.UE \c
.PP
NOTE: Only the direct dependencies for the core logic are listed here
Please contact the project maintainer if you are missing from the list.
.SS RELATED LINKS
.IP \(bu 2
\f[B]Project Repository\f[R]: \c
.UR https://github.com/sayanarijit/qrcode.show
.UE \c
.IP \(bu 2
\f[B]Project Maintainer\f[R]: \c
.UR https://arijitbasu.in
.UE \c
.SS COPYRIGHT
© Arijit Basu 2026
