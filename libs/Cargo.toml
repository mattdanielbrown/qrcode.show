[package]
name = "libs"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
qrcode = { version = "0.14", default-features = false, features = [
  "svg",
  "image",
] }
image = "0.25"
csscolorparser = "0.8"
